// 1. Создание модуля (module.js)

// 2. Импортируем наш модуль

import { PI, circumference, area } from './module.js';

// 3. Использование переменных в нашем файлы

console.log(PI);

const result1 = circumference(12);
const result2 = area(10);

console.log(result1);
console.log(result2);
